@model IEnumerable<TesisProj.Areas.Modelo.Models.Parametro>

@{
    ViewBag.Title = "Parámetros: " + ViewBag.Proyecto;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Parámetros de @ViewBag.Elemento (@ViewBag.Proyecto)</h2>

<p>
    <br />
    <a class="btn btn-inverse" style="color: #E6F0F2"href="@Url.Action("CreateParametro", new { idElemento = ViewBag.ElementoId })"">
  	    <i class="icon-plus icon-white"></i>Nuevo
    </a>
    <a style="margin:5px"></a>
    <a class="btn btn-inverse" style="color: #E6F0F2;"href="@Url.Action("Programa", new {id = ViewBag.ElementoId })"">
  	    <i class="icon-film icon-white"></i>Ver resultados
    </a>
    <a style="margin:5px"></a>
    <a class="btn btn-inverse" style="color: #E6F0F2;"href="@Url.Action("Cuaderno", new { id = ViewBag.ElementoId })"">
  	    <i class="icon-list icon-white"></i>Fórmulas
    </a>
    <a style="margin:5px"></a>
    <a class="btn btn-inverse" style="color: #E6F0F2;"href="@Url.Action("VerParametros", new { id = ViewBag.ElementoId })"">
  	    <i class="icon-flag icon-white"></i>Ver valores
    </a>
    <a style="margin:5px"></a>
    <a class="btn btn-inverse" style="color: #E6F0F2;"href="@Url.Action("SetParametros", new { id = ViewBag.ElementoId })"">
  	    <i class="icon-pencil icon-white"></i>Editar
    </a>
    <a style="margin:5px"></a>
    <a class="btn btn-inverse" style="color: #E6F0F2"href="@Url.Action("Journal", "Proyecto", new { Area = "Modelo",id = ViewBag.ProyectoId   })"">
  			<i class="icon-arrow-left icon-white"></i>Regresar
    </a>
    <br /><br /> 
</p>
<table class="table table-hover table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Referencia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdElemento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdTipoParametro)
        </th>
        <th style="text-align: center">
            @Html.DisplayNameFor(model => model.Sensible)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Referencia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Elemento.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoParametro.Nombre)
        </td>
        <td style="text-align: center">
            @Html.DisplayFor(modelItem => item.Sensible)
        </td>
        <td>
            @Html.ActionLink("Editar", "EditParametro", new { id=item.Id }) |
            @Html.ActionLink("Eliminar", "DeleteParametro", new { id = item.Id }, new { onclick="return DeleteConfirm()" })
        </td>
    </tr>
}

</table>

<p>
    <br />
    <a class="btn btn-inverse" style="color: #E6F0F2"href="@Url.Action("Journal", "Proyecto", new { Area = "Modelo",id = ViewBag.ProyectoId   })"">
  			<i class="icon-arrow-left icon-white"></i>Regresar
    </a>
</p> 

<script>
    function DeleteConfirm(){
        if (confirm("¿Está seguro que desea eliminar el registro?"))
            return true;
        else
            return false;     
    }
</script>